import Link from 'next/link'
import { ProductShell, TopicChips, LearningCard } from '@/components/product-shell'
import { learningCards } from '@/lib/learning-data'

export default function ExplorePage() { return <ProductShell title="Explore"><section className="explore-intro"><p className="eyebrow">BROWSE BY THEME</p><h2>Find a thread<br />worth following.</h2><p>Choose a theme when you know what you&apos;re looking for. Otherwise, your daily reading is waiting on Home.</p><TopicChips /></section><section className="product-section explore-readings"><div className="section-row"><div><p className="eyebrow">THE ARCHIVE</p><h2>All readings</h2></div><Link href="/search">Search all</Link></div><div className="learning-grid">{learningCards.map(item => <LearningCard key={item.title} {...item} />)}</div></section><section className="depth-ladder"><p className="eyebrow">GO DEEPER</p><h2>One idea. More context.</h2><p className="depth-description">Start with a short reading, then follow the thread toward context and the source.</p><div className="depth-steps">{['30 sec · Discover','2 min · Understand','5 min · Explore','Source · Read deeper'].map((step, index) => <span key={step}><b>{String(index + 1).padStart(2, '0')}</b>{step}</span>)}</div></section></ProductShell> }
