import Link from 'next/link'
import { ProductShell, LearningCard } from '@/components/product-shell'
import { learningCards } from '@/lib/learning-data'
export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const { q = '' } = await searchParams; return <ProductShell title="Search"><form className="search-form"><input name="q" defaultValue={q} placeholder="Search ideas, people, books..." aria-label="Search Yadesh" /><button>Search</button></form><section className="product-section"><div className="section-row"><h2>{q ? `Results for “${q}”` : 'Start with a question'}</h2></div><div className="search-groups"><span>TOPICS</span><span>PEOPLE</span><span>BOOKS</span><span>IDEAS</span><span>STORIES</span></div><div className="learning-grid">{learningCards.map(item => <LearningCard key={item.title} {...item} />)}</div></section><p className="source-note">Read. Keep. Remember. Every source-sensitive item is labelled by its context.</p></ProductShell> }
